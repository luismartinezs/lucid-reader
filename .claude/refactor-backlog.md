media/reader.css:60-225 | The paper palette is written out three times (`:root`, `[data-theme='paper']`, `[data-theme='auto']`) and the dark palette twice (`[data-theme='dark']`, `[data-theme='auto'].vscode-dark`). Adding `--heading-fg` and fixing contrast meant editing the same values in five places, and a copy left behind is silently wrong. | Declare each palette once under a `[data-palette]` attribute and have reader.js map theme name plus VS Code theme kind to a palette name, so `auto` selects an existing palette instead of duplicating one.
